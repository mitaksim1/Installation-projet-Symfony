<form method="post" class="form-delete" action="{{ path('plat_delete', {'id': plat.id}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plat.id) }}">
    <button class="btn btn-danger btn-style">Delete</button>
</form>
